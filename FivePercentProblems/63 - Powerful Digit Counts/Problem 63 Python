# 3.52 ms
# this doesnt have a was to actually be sure that is the max, i just guessed based on math

counter = 0
for i in range(1, 100):
  for j in range(1, 100):
    if len(str(i**j)) > j:
      break
    if len(str(i**j)) == j:
      counter += 1
print(counter)
