
# i realize how inefficient algorithm is, but i have a computer so i'm gonna use it.

notdone = True
spn = 20

while notdone:
    for x in range(19, 2, -1): # don't need 2 or 1
        if spn % x == 0:
            if x == 3:
                notdone = False
            continue
        else:
            break
    spn += 20
    
print(spn - 20) # minus twenty because last increment in while loop
